<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bitcamp.web.mapper.OrderMapper">

    <insert id="insertOrder" parameterType="com.bitcamp.web.domain.OrderDTO">
        insert into orders(order_id, customer_id, employee_id, order_date, shipper_id) 
        values(#{orderId}, #{customerId}, #{employeeId}, #{orderDate}, #{shipperId});
    </insert>

    <select id="selectOrders" parameterType="com.bitcamp.web.domain.OrderDTO"
     resultType="com.bitcamp.web.domain.OrderDTO">
        select * from orders
    </select>

    <select id="selectOrdersByOption" parameterType="com.bitcamp.web.domain.OrderDTO"
     resultType="com.bitcamp.web.domain.OrderDTO">
        select * from orders where customer_id = #{customerId}
    </select>

    <select id="selectOrderByOrderId" parameterType="string"
     resultType="com.bitcamp.web.domain.OrderDTO">
        select * from orders where order_id = #{value}
    </select>

    <update id="updateOrder" parameterType="com.bitcamp.web.domain.OrderDTO">
        update orders set
            customer_id = #{customerId},
            employee_id = #{employeeId},
            order_date = #{orderDate},
            shipper_id = #{shipperId}
        where order_id = #{orderId}
    </update>

    <delete id="deleteOrder" parameterType="com.bitcamp.web.domain.OrderDTO">
        delete from orders where order_id = #{orderId}
    </delete>

</mapper>